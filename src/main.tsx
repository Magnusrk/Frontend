import React from 'react'
import ReactDOM from 'react-dom/client'
import AlbumPicker from './App.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    
	<AlbumPicker />
  </React.StrictMode>,
)
